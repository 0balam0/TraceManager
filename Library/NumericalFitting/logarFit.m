function v = logarFit(x,y)


% fit di funzione del tipo:
% y = a * log(b*x),
% che trasformo in retta doppio logaritmica??
% exp(y) = log(a) + log(x) + log(log(
%
% INPUT:
% x, y: dati sparsi
%
% OUTPUT:
% v: coefficienti del fit, in ordine descrescente


disp('TODO: da fare!!')

return