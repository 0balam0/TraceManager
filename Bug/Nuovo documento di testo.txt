disattivata funzione function disattivaEditSelez(handles), Non funzionava
    inutile per il momento

add:
    in function lb_exp_Callback
      if ~(handles.lb_exp.String{1} == ' ') per evitare errori di aggiornamento finestre etc

    in: function lb_Ax_Callback(hObject, eventdata, handles)  
            attivaEditSelez(handles)
            disattivaEditSelez(handles)
    in
        


create: 
    function attivaEditSelez(handles) %riattiva le finestre delle operazioni se disattivate